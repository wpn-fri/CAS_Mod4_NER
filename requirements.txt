# Core dependencies for transformer-based NER project
# Python 3.8+ recommended

# Hugging Face ecosystem
transformers>=4.30.0
datasets>=2.12.0
tokenizers>=0.13.0

# PyTorch (CPU version - for GPU, see installation notes below)
torch>=2.0.0
accelerate>=0.26.0
# For GPU support on Windows, use: pip install torch --index-url https://download.pytorch.org/whl/cu118

# NER evaluation metrics
seqeval>=1.2.2

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Machine learning utilities
scikit-learn>=1.2.0

# Progress bars
tqdm>=4.65.0

# Jupyter notebook support
jupyter>=1.0.0
ipywidgets>=8.0.0

# Optional but recommended
sentencepiece>=0.1.99  # For some tokenizers
protobuf==3.20.0

# Installation notes:
# -------------------
# 1. CPU-only installation:
#    pip install -r requirements.txt
#
# 2. GPU installation (NVIDIA CUDA 11.8):
#    pip install torch --index-url https://download.pytorch.org/whl/cu118
#    pip install -r requirements.txt
#
# 3. Mac with Apple Silicon (M1/M2):
#    pip install -r requirements.txt
#    # PyTorch will use MPS (Metal Performance Shaders) automatically
